import { View, Text, ScrollView } from "react-native";

function PlaceForm() {
  return (
    <ScrollView>
      <View>
        <Text>Title</Text>
      </View>
    </ScrollView>
  );
}

export default PlaceForm;
